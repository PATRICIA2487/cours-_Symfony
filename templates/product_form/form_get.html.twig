{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">
        {% for product in products %}
            {{product.productName}}
        {% endfor %}
    </div>

{% endblock %}
